import { Meteor } from 'meteor/meteor';
import { Events, Users } from '../imports/api/collections.js';
import '../imports/api/servermethods.js';

Meteor.startup(() => {
  // code to run on server at startup
});
